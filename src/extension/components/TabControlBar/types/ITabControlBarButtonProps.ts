// types
import type { TProps as IIconButtonProps } from '@common/components/IconButton';

interface ITabControlBarButtonProps {
  button: IIconButtonProps;
  tooltipLabel?: string;
}

export default ITabControlBarButtonProps;
